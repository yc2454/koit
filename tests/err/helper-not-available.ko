// expect: not available in a `syscall` program
// From spec/language.md 13: the call table says where a call is
// available; `redirect` is a packet helper.
program p : syscall {
  let v = redirect(1)?
  return 0
}
