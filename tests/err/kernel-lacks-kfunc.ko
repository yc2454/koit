// expect: is not on kernel v6.8: v6.8 has no bpf_preempt_disable
// A resource declaration the target kernel lacks: the diagnostic names the
// kernel and the kernel function it is missing, from the join of
// the koit side with the transcribed kernel side.
program p : syscall {
  hold preempt_off { let x = 1 }
  return 0
}
