// expect: not packet-representable
// From spec/language.md 7: the type of a view is packet-representable,
// without `spinlock`.
type Entry = { lk: spinlock, n: u64 }

program p : xdp
  default { drop }
{
  let e = pkt.view<Entry>(0)?
  drop
}
