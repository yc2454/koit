// expect: nothing else
// From spec/language.md 7: byte-order integers support `==`, `!=`,
// loads, and stores, and nothing else.
type EthHdr = { dst: u8[6], src: u8[6], proto: be16 }

program p : xdp fail drop {
  let eth = pkt.view<EthHdr>(0)?
  let x = eth.proto + 1
  drop
}
