// expect: has no packet
// From spec/language.md 13: `pkt` exists in packet kinds only.
type EthHdr = { dst: u8[6], src: u8[6], proto: be16 }

program p : syscall {
  let eth = pkt.view<EthHdr>(0)?
  return 0
}
