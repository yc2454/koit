// pending: dynptrs
// selftest: progs/dynptr_fail.c skb_invalid_data_slice1 (T0122 to T0135, T0159 to T0161, rule 10.21, master fe2ec83)
// A slice of a packet dynptr used after a helper that may change
// packet data, or after a write through the metadata dynptr, or, for
// a clone's slice, after either. The slice has two guards: the
// dynptr it was cut from and the packet's layout token (entry 51,
// guard sets). Placeholder until the dynptr declarations exist. In C:
//
//   bpf_dynptr_from_skb(skb, 0, &ptr);
//   hdr = bpf_dynptr_slice(&ptr, 0, buffer, sizeof(buffer));
//   bpf_skb_pull_data(skb, skb->len);
//   val = hdr->h_proto;                 /* dead */
